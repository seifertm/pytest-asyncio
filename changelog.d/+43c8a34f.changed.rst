Pytest runs tests in a synchronous fashion which means that async fixtures need to be synchronized at some point. Starting with this version async fixtures are synchronized during fixture setup rather than at collection time. This reduces the test collection time at the expense of test runtime. This makes it more convenient to select a subset of tests, especially in large code bases.
